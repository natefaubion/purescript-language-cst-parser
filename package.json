{
  "private": true,
  "scripts": {
    "integration": "spago -x integration/integration.dhall run",
    "bench-file": "spago -x bench/bench.dhall build && node --expose-gc -e \"require('./output/BenchFile/index.js').main()\""
  },
  "devDependencies": {
    "purescript": "^0.14.0",
    "spago": "^0.19.1"
  }
}
